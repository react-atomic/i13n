{
  "description": "i13 client",
  "keywords": [
    "i13n"
  ],
  "author": "Hill <hill@kimo.com>",
  "license": "MIT",
  "dependencies": {
    "i13n": "*"
  },
  "devDependencies": {
    "babel-cli": "*",
    "reshow-app": "*"
  },
  "scripts": {
    "clean": "find ./build ./assets -name '*.*' | xargs rm -rf && mkdir -p build",
    "build": "npm run clean && BABEL_ENV=build babel src -d build/src",
    "prepublishOnly": "npm run build"
  }
}
